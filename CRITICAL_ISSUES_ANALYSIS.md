# Анализ критических проблем аддона SotA

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. Отсутствие обработки ошибок в критических местах

**Проблема:** Команды обрабатываются без `pcall()`, что может привести к падению аддона.

**Место:** `sota-dashboard.lua:121-122`
```lua
SlashCmdList["SOTA_DEFAULT_COMMAND"] = function(msg)
	SOTA_HandleSOTACommand(msg);  -- ❌ Нет обработки ошибок
end
```

**Риск:** При ошибке в команде аддон "зависает" и требует `/reload`.

**Решение:** ✅ Уже исправлено ранее - нужно проверить, что pcall добавлен.

---

### 2. Race Condition в JobIsRunning

**Проблема:** Флаг `JobIsRunning` может быть установлен, но не сброшен при ошибке.

**Место:** `sota-core.lua:425-438`
```lua
if not JobIsRunning then	
	JobIsRunning = true
	
	local job = SOTA_GetNextJob()
	while job do
		job[1](job)  -- ❌ Если здесь ошибка, JobIsRunning останется true
		job = SOTA_GetNextJob()
	end
	
	JobIsRunning = false  -- ❌ Не выполнится при ошибке
end
```

**Риск:** После ошибки все последующие DKP операции будут заблокированы.

**Решение:** Использовать `pcall()` или `xpcall()` с гарантированным сбросом флага.

---

### 3. Небезопасный парсинг строк в сетевых сообщениях

**Проблема:** Парсинг сетевых сообщений без валидации может привести к ошибкам.

**Место:** `sota-dashboard.lua:1092`
```lua
local _, _, timestamp, tid, author, description, transstatus, name, dkp = string.find(message, "([^/]*)/([0-9]*)/([^/]*)/([^/]*)/([0-9]*)/([^/]*)/([^/]*)")

tid = tonumber(tid);  -- ❌ Может быть nil
```

**Риск:** Если `string.find` не найдет совпадений, переменные будут `nil`, что приведет к ошибкам.

**Решение:** Проверять результат `string.find` перед использованием.

---

### 4. Проблема с логическим оператором в условии

**Проблема:** Неправильное использование логического оператора.

**Место:** `sota-core.lua:808`
```lua
if not table.getn(transaction[6]) == 1 then  -- ❌ ОШИБКА ЛОГИКИ!
```

**Проблема:** `not table.getn(...) == 1` эквивалентно `(not table.getn(...)) == 1`, что всегда `false`.

**Должно быть:**
```lua
if table.getn(transaction[6]) ~= 1 then
```

**Риск:** Логика работает неправильно, может привести к некорректному поведению.

---

### 5. Отсутствие проверки на nil при доступе к таблицам

**Проблема:** Множественные места, где доступ к элементам таблиц без проверки.

**Примеры:**
- `sota-core.lua:490` - `GuildRosterTable[n]` без проверки
- `sota-dashboard.lua:1111` - `SOTA_transactionLog[tid]` может быть nil
- `sota-auction.lua` - доступ к `IncomingBidsTable` без проверок

**Риск:** `attempt to index a nil value` ошибки.

---

### 6. Проблема с синхронизацией данных между клиентами

**Проблема:** Нет проверки целостности данных при синхронизации.

**Место:** `sota-dashboard.lua:1092-1126`
```lua
local transaction = SOTA_transactionLog[tid];
if not transaction then
	transaction = { timestamp, tid, author, description, transstatus, { } };
end
-- ❌ Нет проверки, что timestamp, tid и т.д. не nil
```

**Риск:** Коррупция данных транзакций при некорректных сетевых сообщениях.

---

### 7. Потенциальная утечка памяти в таблицах

**Проблема:** Таблицы могут расти бесконечно без очистки.

**Места:**
- `SOTA_transactionLog` - никогда не очищается автоматически
- `SOTA_HISTORY_DKP` - может расти бесконечно
- `GuildRosterTable` - пересоздается, но старые ссылки могут остаться

**Риск:** Утечка памяти при длительной игре.

---

### 8. Небезопасное преобразование типов

**Проблема:** Множественные места с `tonumber()` без проверки результата.

**Пример:** `sota-core.lua:484`
```lua
if not dkp or not tonumber(dkp) then
	dkp = 0;
end
-- ❌ tonumber() может вернуть 0, что тоже falsy в Lua 5.0
```

**Проблема:** В Lua 5.0 `tonumber("0")` вернет `0`, что является falsy значением.

**Решение:** Использовать `tonumber(dkp) ~= nil` или проверять явно.

---

### 9. Проблема с доступом к глобальным переменным

**Проблема:** Использование `getglobal()` может вернуть `nil` без проверки.

**Места:** Множественные в `sota-options.lua`:
```lua
getglobal("FrameConfigBiddingMSoverOSPriority"):SetChecked(...)  -- ❌ Может быть nil
```

**Риск:** Ошибка, если UI элемент не создан.

---

### 10. Отсутствие валидации входных данных от пользователя

**Проблема:** Команды принимают любые данные без валидации.

**Пример:** `sota-dashboard.lua:279-294`
```lua
if cmd == "raid" then
	sign = string.sub(arg, 1, 1);
	if sign == "-" then
		arg = string.sub(arg, 2);
		return SOTA_Call_SubtractRaidDKP(arg);  -- ❌ arg может быть nil или пустым
```

**Риск:** Ошибки при некорректных командах.

---

## ⚠️ ВАЖНЫЕ ПРОБЛЕМЫ

### 11. Проблема с индексацией таблиц

**Проблема:** Использование `table.getn()` для таблиц с нечисловыми индексами.

**Риск:** `table.getn()` работает только для массивов с числовыми индексами от 1 до n.

**Решение:** Проверять структуру таблицы перед использованием `table.getn()`.

---

### 12. Проблема с обработкой пустых строк

**Проблема:** Множественные места, где пустые строки не обрабатываются корректно.

**Пример:** `sota-core.lua:475`
```lua
if not note or note == "" then
	note = "<0>";
end
```

**Хорошо, но:** Нужно проверять во всех местах парсинга.

---

### 13. Проблема с синхронизацией очереди рейда

**Проблема:** Нет проверки на дубликаты при синхронизации очереди.

**Место:** `sota-dashboard.lua:1269-1307`

**Риск:** Дублирование записей в очереди.

---

## 📊 Приоритет исправлений

| Приоритет | Проблема | Критичность | Сложность |
|-----------|----------|-------------|-----------|
| 🔴 P0 | #4 - Логическая ошибка в условии | Критично | Низкая |
| 🔴 P0 | #2 - Race Condition в JobIsRunning | Критично | Средняя |
| 🔴 P0 | #3 - Небезопасный парсинг сетевых сообщений | Критично | Средняя |
| 🟠 P1 | #5 - Отсутствие проверок на nil | Высокая | Средняя |
| 🟠 P1 | #8 - Небезопасное преобразование типов | Высокая | Низкая |
| 🟡 P2 | #7 - Утечка памяти | Средняя | Высокая |
| 🟡 P2 | #9 - Доступ к глобальным переменным | Средняя | Средняя |
| 🟢 P3 | #10 - Валидация входных данных | Низкая | Средняя |
