# 🔧 Исправления v6

## ✅ Исправлено:

### 1. **КРИТИЧНО: Ошибка при начислении DKP за босса** 🐛
**Ошибка:** `attempt to perform arithmetic on local 'dkp' (a nil value)`

**Проблема:** Функция `SOTA_AddRaidDKP` не обрабатывала случай когда передается только число без имени босса

**Исправление:**
```lua
-- До (ОШИБКА):
function SOTA_AddRaidDKP(arg, silentmode, callMethod)
    local _, _, dkp, boss = string.find(arg, "(%S+)%s+(.+)");
    dkp = 1 * dkp;  // ← dkp = nil если нет второго слова!
end

-- После (РАБОТАЕТ):
function SOTA_AddRaidDKP(arg, silentmode, callMethod)
    local dkp, boss;
    local _, _, dkpStr, bossName = string.find(arg, "(%S+)%s+(.+)");
    
    if dkpStr then
        dkp = tonumber(dkpStr);
        boss = bossName;
    else
        -- Только число без имени босса
        dkp = tonumber(arg);
        boss = "DKP";
    end
    
    if not dkp then
        localEcho("Ошибка: неверный формат DKP");
        return false;
    end
    // Продолжение...
end
```

**Теперь работает:**
- ✅ `/sota raid +100 Ragnaros` → 100 DKP + имя "Ragnaros"
- ✅ `/sota raid +100` → 100 DKP + имя "DKP"
- ✅ Кнопка 💰 (монета) → работает без ошибок!

---

### 2. **Иконки переключателя DKP Notes** 🎨

**Было:**
- ❌ INV_Misc_Book_11 (книга)
- ❌ INV_Sword_39 (Thunderfury)

**Стало:**
- ✅ INV_Staff_Medivh (Atiesh) = Officer Notes
- ✅ INV_Misc_Gem_Pearl_05 (жемчужина) = Public Notes

**Правильные ID:**
```
Officer Notes: inv_staff_medivh (ID: 135226)
Public Notes:  inv_misc_gem_pearl_05 (ID: 134124)
```

---

### 3. **Раздел Bidding - полная русификация** 🌍

**Переведено:**
- "Auction Time: X seconds" → "Время аукциона: X сек"
- "Auction Extension: X seconds" → "Продление аукциона: X сек"
- "Enable MS/OS bidding..." → "Включить MS/OS ставки..."
- "Enable Raid Queue Zone Check" → "Проверка зоны для очереди рейда"
- "Enable Raid Queue Online Check" → "Проверка онлайна для очереди рейда"
- "Allow players to pass" → "Разрешить игрокам паснуть"
- "Disable (hide) Dashboard" → "Скрыть Dashboard"

---

### 4. **Раздел Misc DKP - русификация** 🌍

**Переведено:**
- "DKP String Length: X" → "Длина строки DKP: X"
- "Minimum DKP penalty: X" → "Минимальный штраф DKP: X"

---

### 5. **Улучшено описание Strategy 0** 📝

**Было:** "Без минимальных правил (+100 ДКП)"  
**Стало:** "Свободные ставки (любая выше текущей)"

**Объяснение Strategy 0:**
```
Текущая ставка: 500 DKP
Следующая может быть: 501, 502, 600, 1000... (любая больше 500)
```

**Сравнение стратегий:**

| Стратегия | Описание | Пример (текущая 500) |
|-----------|----------|---------------------|
| 0: Свободные | Любая выше текущей | 501, 550, 600... |
| 1: +100 ДКП | Минимум +100 | 600, 700, 800... |
| 2: +10% | Минимум +10% | 550, 605, 665... |

---

## 📊 Итоговое состояние аддона:

### Dashboard (4 кнопки):
```
[👥 Очередь]  [💀 За босса]  [💰 Всем]  [💎📕 Режим]

💎 = Public Notes (жемчужина)
📕 = Officer Notes (Atiesh)
```

### Разделы настроек:
```
[Bidding] [Boss DKP] [Item DKP] [Misc DKP] [Messages]
   ↑          ↑          ↑           ↑
  RUS       RUS       Таблица      RUS
```

### Bid Strategies (осталось 3):
```
○ Свободные ставки
○ +100 ДКП
○ +10%
```

**Все на русском языке!** ✅
